// Configure this with your actual backend URL when deploying
// e.g. "https://your-backend.onrender.com"
// Leave empty if frontend and backend are on the same domain or if using a proxy.
window.NETPULSE_API_BASE = ""; 
